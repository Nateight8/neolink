"use client";

import NotFoundClient from "@/components/not-found/v2";

export default function NotFound() {
  return (
    <div className="min-h-screen w-full overflow-hidden">
      <NotFoundClient />
    </div>
  );
}
